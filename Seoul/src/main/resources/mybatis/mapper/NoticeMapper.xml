<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sp.app.mapper.NoticeMapper">
	<sql id="where-list">
		<if test="schType=='all' ">
			<![CDATA[
				(
					INSTR(subject, #{kwd}) > 0
					OR INSTR(REGEXP_REPLACE(content, '<[^>]+>', ''), #{kwd}) > 0
				)
			]]>
		</if>
		<if test="schType=='subject' ">
			INSTR(subject, #{kwd}) &gt; 0
		</if>
		<if test="schType=='content' ">
			<![CDATA[
				INSTR(REGEXP_REPLACE(content, '<[^>]+>', ''), #{kwd}) > 0
			]]>
		</if>
		<if test="schType=='reg_date' ">
			( INSTR(TO_CHAR(reg_date, 'YYYYMMDD'), #{kwd}) &gt; 0
				OR TO_CHAR(reg_date, 'YYYY-MM-DD') = #{kwd} )
		</if>
	</sql>
	
	<!-- 공지글 리스트 -->
	<select id="listNoticeTop" resultType="com.sp.app.model.Notice">
		SELECT n.num, notice, subject, hit_count, 
			TO_CHAR(reg_date, 'YYYY-MM-DD') reg_date,
			TO_CHAR(n.modify_date, 'YYYY-MM-DD') modify_date,
			NVL(fileCount, 0) fileCount
		FROM notice n
		LEFT OUTER JOIN (
			SELECT num, COUNT(*) fileCount
			FROM notice_File
			GROUP BY num
		) f ON n.num = f.num
		WHERE notice = 1 AND showNotice = 1
		ORDER BY modify_date DESC
	</select>
	
	<!-- 데이터 개수 -->
	<select id="dataCount" parameterType="map" resultType="Integer">
		SELECT NVL(COUNT(*), 0)
		FROM notice n
		JOIN member m ON n.member_id = m.member_id
		<where>
			showNotice = 1
			<if test="kwd != null and kwd != '' ">
				AND <include refid="where-list"/>
			</if>
		</where>
	</select>
	
	<!-- 리스트 -->
	<select id="listNotice" parameterType="map" resultType="com.sp.app.model.Notice">
		SELECT n.num, notice, subject, hit_count,
			reg_date, n.modify_date, NVL(fileCount, 0) fileCount
		FROM notice n
		LEFT OUTER JOIN (
			SELECT num, COUNT(*) fileCount
			FROM notice_File
			GROUP BY num
		) f ON n.num = f.num
		<where>
			showNotice = 1
			<if test="kwd != null and kwd != '' ">
				AND <include refid="where-list"/>
			</if>
		</where>
		ORDER BY modify_date DESC
		OFFSET #{offset} ROWS FETCH FIRST #{size} ROWS ONLY
	</select>
	
	<!-- 글보기 -->
	<select id="findById" parameterType="Long" resultType="com.sp.app.model.Notice">
		SELECT num, subject, content,
			TO_CHAR(reg_date, 'YYYY-MM-DD HH24:MI:SS') reg_date,
			TO_CHAR(n.modify_date, 'YYYY-MM-DD HH24:MI:SS') modify_date,
			hit_count, notice, showNotice
		FROM notice n
		WHERE num = #{num} AND showNotice = 1	
	</select>
	
	<!-- 조회수 증가 -->
	<update id="updateHitCount" parameterType="Long">
		UPDATE notice SET hit_count = hit_count + 1
		WHERE num = #{num}
	</update>
	
	<!-- 이전글 -->
	<select id="findByPrev" parameterType="map" resultType="com.sp.app.model.Notice">
		SELECT num, subject
		FROM notice n
		<where>
			showNotice = 1
			<if test="kwd != null and kwd != '' ">
				AND<include refid="where-list"/>
			</if>
			AND(modify_date &gt; TO_DATE(#{modify_date}, 'YYYY-MM-DD HH24:MI:SS'))
		</where>
		ORDER BY modify_date ASC
		FETCH FIRST 1 ROWS ONLY
	</select>
	
	<!-- 다음글 -->
	<select id="findByNext" parameterType="map" resultType="com.sp.app.model.Notice">
		SELECT num, subject
		FROM notice n
		<where>
			showNotice = 1
			<if test="kwd != null and kwd != '' ">
				AND<include refid="where-list"/>
			</if>
			AND(modify_date &lt; TO_DATE(#{modify_date}, 'YYYY-MM-DD HH24:MI:SS'))
		</where>
		ORDER BY modify_date DESC
		FETCH FIRST 1 ROWS ONLY
	</select>
	
	<select id="listNoticeFile" parameterType="Long" resultType="com.sp.app.model.Notice">
		SELECT fileNum, num,save_filename, original_filename, filesize
		FROM notice_File
		WHERE num = #{num}
	</select>
	
	<select id="findByFileId" parameterType="Long" resultType="com.sp.app.model.Notice">
		SELECT fileNum, num, save_filename, original_filename, filesize
		FROM notice_File
		WHERE fileNum = #{fileNum}
	</select>
</mapper>